package build

var (
	// Name is the name of the application.
	Name = "D&D Machine"
	// Version is the current build version of the binary.
	Version = "unknown"
	// Commit is the specific commit the binary is build from.
	Commit = "unknown"
	// Branch is either the feature or the main branch the binary is build from.
	Branch = "unknown"
	// Timestamp is the date time when the binary was build at.
	Timestamp = "unknown"
)
